const assert = require('assert')
describe('DeepLint Run', function () {
  describe('#indexOf()', function () {
    it('should return -1 when the value is not present', function () {
      assert.strict.equal([1, 2, 3].indexOf(4), -1)
    })
  })
})
